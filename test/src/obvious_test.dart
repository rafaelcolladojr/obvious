// ignore_for_file: prefer_const_constructors

import 'package:flutter_test/flutter_test.dart';
import 'package:obvious/obvious.dart';

void main() {
  group('Obvious', () {
    test('can be instantiated', () {
      expect(Obvious(), isNotNull);
    });
  });
}
